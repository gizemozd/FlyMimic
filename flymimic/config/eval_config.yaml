# Default config for evaluation
defaults:
  - _self_

# Paths
model_path: "../logs/ppo_mocap_agent_muscle_seed_arm_damping_stiff_0_2025-08-09_23-28-29_14000000_steps.zip"
videos_dir: "../results/videos"
figures_dir: "../results/figures"
data_dir: "../results/rollout_data"

# Run settings
play: false
record_video: true
rollout_steps: 210
camera_id: 0
fps: 30
render_every_n_steps: 1
use_viewer: false
show_plot: false
headless: true  # set true on a server without display

# Model XML selection override (normally auto)
xml_name: null
